# Handoff 2026-05-06

Purpose: 다음 세션이 이번 product-web 학습 표시층 정리 이후 바로 이어받기 위한 짧은 기준.

## Current Baseline
- Latest commit: `4eeec27 Recognize natural format locks in prompt learning`
- Working tree was clean after the latest commit.
- Current browser verification surface remains `apps/product-web`.
- Recent scope stayed in the app display / learning layer only.

## Settled This Session
- `다음 입력 개선 힌트`에 단순 힌트 조각 복사 버튼을 붙이는 것은 제품적으로 의미가 약해 revert했다.
- 입력 개선 힌트는 복사 액션보다, 다음 자유 입력에 응용할 수 있는 한 줄 예시로 읽히는 편이 낫다.
- `product-web-manual-checklist.md` now records that weak copy actions should not be added to hint fragments.
- Prompt learning panel now recognizes natural LLM format-lock wording such as `목록 형태`, `범주별로`, `항목별로`, `체크리스트 형식`, and JSON/Markdown format wording.

## Verified
- `npm.cmd run typecheck`
- `npm.cmd run verify:golden-cases`
- `npm.cmd run verify:prompt-cycle`
- `npm.cmd run build:product-web`
- Browser check with Gemini selected: same prompt request showed actionable input hints and `출력 형식 고정하기` as `이번 적용`.

## Suggested Next Start
Start with product-web manual verification rather than another immediate feature.

Recommended first pass:
1. Run the 6-item minimum set in `docs/product-web-manual-checklist.md`.
2. Use LLM-connected product-web if available.
3. Note any mismatch between visible learning copy and actual LLM output.

## Do Not Expand Silently
- Do not re-add copy buttons to input improvement hints unless the copied text is a complete, meaningful next-input sentence.
- Do not change renderer policy, approval behavior, engine-core contracts, or LLM output contracts from this handoff alone.
- Do not move into broader follow-up chaining or freeform follow-up instructions without a new boundary decision.
